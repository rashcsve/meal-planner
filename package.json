{
  "name": "meal-planner",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "api",
    "web",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev -w api",
    "dev:all": "docker compose -f api/docker-compose.yml up -d && concurrently -n api,web -c blue,green \"npm run dev -w api\" \"npm run dev -w web\"",
    "test": "npm run test -w api && npm run test -w web",
    "typecheck": "npm run typecheck -w api && npm run typecheck -w web",
    "lint": "npm run lint -w api && npm run lint -w web",
    "migrate": "npm run migrate -w api"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
